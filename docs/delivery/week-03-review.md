# Week 3 PR 与 AI Code Review

审查日期：2026-09-25。审查者：Codex；性质：作者侧 AI 自查，不能替代独立人工审核。审查基线为主分支 `3a1b440`，实现快照 `4081cb9`，接入第二周历史后的合并提交 `61cb44b`。

## 审查范围与结论

检查 CSV 输入边界和规范化、上传路径及大小限制、Redis 投递与 Worker 认领、批次事务与失败回写、状态和计数一致性、错误 API 的查询隔离与公开字段、React 请求取消/轮询/错误展示、数据库迁移及发布包内容。

未发现需要阻止本地 V1.0 验收的新代码问题。这个结论仅覆盖当前本地单机用途，不代表完成生产安全审计或可靠性保证。已有完整测试与浏览器证据分别见 [测试报告](week-03-tests.md)、[部署验收](week-03-release.md)。

## 发现的问题与处理

| 发现 | 处理与验证 |
| --- | --- |
| 第三周分支从第一周分出，缺少主分支已合并的第二周历史 | 合并 origin/main，逐文件核对冲突；保留第三周真实 Worker、接口、页面和测试，补回第二周 PR 文档、日志和截图。合并后业务代码与 `4081cb9` 完全一致。 |
| 合并自动追加了重复的 UPLOAD_DIR 和 REDIS_JOB_QUEUE | 删除末尾重复项，保留当前配置分组和解释；Compose 配置检查通过。 |
| 演示截图显示 SUCCESS 但计数全为 0 | 此前已确认正在运行旧版模拟 Worker；更新开发服务后重新导入同一正常样例为 5/5/0，数据库成功行数为 5。保留原历史任务，不伪造旧统计。正常业务回归再次通过。 |
| 先前测试报告可能遗漏 subTest 失败、发布包可能带入系统缓存文件 | 第七、八节已修复报告状态归类和打包排除规则；报告自检纳入本次 74 项测试，发布前再次校验包内清单。 |

## 合并后验证

- `sh scripts/check.sh` 退出码 0：Ruff、CSV 14 项、前端 6 项、TypeScript、构建、Compose 配置及 OpenAPI 一致性通过。
- 独立测试 MySQL/Redis 运行当前代码：74 项测试全部通过，覆盖真实 HTTP/Worker、数据库约束与事务故障、数据和错误计数。
- [检查原始输出](evidence/week-03-review/checks.txt)、[后端测试摘要](evidence/week-03-review/tests.txt)。CSV 14 项包含在后端 74 项内，不重复计数。
- 本次合并没有修改已验收的业务/部署代码，沿用此前 9 组浏览器验收和 3 类样例对账证据；没有宣称本次重复执行浏览器验收。

## 已知限制与交接

BLPOP 没有确认/崩溃恢复；Worker 单进程串行；重试等预留配置尚未生效；前端限制固定 10 MB；无认证和公网部署方案。已在 Release Notes 明示，V1.1 通过新 Issue 讨论恢复语义。

PR、Review、发布草稿与后续 Issue 入口统一记录于 [第九节交付记录](week-03-handoff.md)。人工审核、本人现场演示、正式发布及标签验证完成后再勾选对应清单项。
